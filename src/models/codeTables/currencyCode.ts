class CurrencyCode {
    code: string;
    currency: string;

    constructor(code: string, currency: string) {
        this.code = code;
        this.currency = currency;
    }
}

export default CurrencyCode;