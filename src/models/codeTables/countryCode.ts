class CountryCode {
    code: string;
    country: string;

    constructor(code: string, country: string) {
        this.code = code;
        this.country = country;
    }
}

export default CountryCode;